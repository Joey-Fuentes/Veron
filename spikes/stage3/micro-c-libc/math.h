/* micro-c: minimal math.h. tcc includes it but the paths that would call into
 * it are float paths micro-c cannot compile yet -- see the float note in
 * ../patches/micro-c-experiments/README.txt. Empty on purpose, and this
 * comment is the record of why. */
#ifndef _MICROC_MATH_H
#define _MICROC_MATH_H
#endif
