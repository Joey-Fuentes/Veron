EXPERIMENT -- see README.txt. Unsound in parts; do not ship.

GENERATED AGAINST THE PIN: bd2fe4b plus patches 0001-0004, and against
EVERY differing file rather than a hand-written list.

--- a/cc_globals.h
+++ b/cc_globals.h
@@ -36,6 +36,11 @@
 /* Make our string collection more efficient */
 extern char* hold_string;
 extern int string_index;
+extern int file_index;
+extern int file_max;
+extern char* file_buffer;
+extern int output_file_index;
+extern char* output_file_buffer;
 
 /* Our Target Architecture */
 extern int Architecture;
@@ -69,3 +74,14 @@
 
 /* feature unsupported by cc_* */
 extern void maybe_bootstrap_error(char* feature);
+
+extern struct type* last_type_base;
+extern int last_type_stars;
+
+void strip_attributes(void);
+
+extern int global_second_dimension;
+
+extern int pending_bitfield_offset;
+extern int pending_bitfield_width;
+extern int pending_bitfield_size;
