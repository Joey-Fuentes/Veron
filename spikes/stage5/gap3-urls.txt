# The packages the dependency scan says are REQUIRED and absent.
#
# 231 declared names are not in the set, and almost all of them are optional:
# platform frameworks that never apply here (appleframeworks, cocoa, android,
# directx), features we disable by policy (x11, xcb, systemd, qt6, gtk+), test
# harnesses (check, cmocka, gtest, valgrind), sub-libraries of packages we
# already have (libbrotlidec is brotli's, hogweed is nettle's, libavcodec is
# ffmpeg's), and a long codec tail nothing on the browser path needs.
#
# THESE ARE THE ONES THAT ARE NOT OPTIONAL:
#
#   gmp     nettle builds hogweed against it, hogweed is nettle's public-key
#           half, and gnutls needs hogweed. Without gmp there is no TLS.
#   libelf  mesa requires it -- the mesa configure spike found this months ago
#           and it was written down and then not acted on.
#   libcap  bubblewrap needs it, and bubblewrap is WPE WebKit's sandbox as
#           well as the tool a user rebuilds Veron with.
#   ogg     gst-plugins-base's audio path. Vorbis is the container's codec and
#   vorbis  ogg is the container; neither is optional for basic playback.
#   scdoc   foot, fuzzel and seatd generate their man pages with it. Small,
#           and cheaper to have than to patch three build files.
#
# AND EACH OF THESE IS PROBED FOR ITS OWN DEPENDENCIES TOO. Adding a package
# to close a gap has twice opened another -- file needed nothing, but bash
# pulled in nothing while glib needed bash, and libudev-zero was itself only
# found by reading declarations. A package added without reading what IT
# declares is the next gap, discovered later and more expensively.

# batch: required
https://ftp.gnu.org/gnu/gmp/gmp-6.3.0.tar.xz
https://sourceware.org/pub/elfutils/0.192/elfutils-0.192.tar.bz2
https://git.kernel.org/pub/scm/libs/libcap/libcap.git/snapshot/libcap-2.76.tar.gz
https://downloads.xiph.org/releases/ogg/libogg-1.3.6.tar.xz
https://downloads.xiph.org/releases/vorbis/libvorbis-1.3.7.tar.xz
https://git.sr.ht/~sircmpwn/scdoc/archive/1.11.3.tar.gz

# batch: required
# The corrected URLs for three packages whose old entries were removed from
# remaining-urls.txt. nss and nspr came from asking archive.mozilla.org rather
# than guessing its layout; libxkbcommon publishes no tarball at all and is
# pinned by commit instead, so it appears in the git-pinned set and not here.
https://archive.mozilla.org/pub/security/nss/releases/NSS_3_126_RTM/src/nss-3.126.tar.gz
https://archive.mozilla.org/pub/nspr/releases/v4.39/src/nspr-4.39.tar.gz

# THE EMULATOR SET (2026-08-24): the five sources the bare-metal qemu
# needs and had no pin for when the recipes were written. Each recipe
# says sha256 = "PENDING"; stage5-mirror-upload fetches these by URL,
# measures them, records the rows, and the digest is pasted into the
# recipe. openssl is NOT here: the set builds the snapshot already in
# src/ (openssl-8cf17aae..., pinned by edk2-src-openssl). A 404 means
# an asset name is wrong; libtpms and swtpm fall back to their tag
# archives plus an autoreconf step.
https://github.com/libsdl-org/SDL/releases/download/release-2.32.10/SDL2-2.32.10.tar.gz
https://gitlab.freedesktop.org/slirp/libslirp/-/archive/v4.9.3/libslirp-v4.9.3.tar.gz
https://download.gnome.org/sources/json-glib/1.10/json-glib-1.10.8.tar.xz
https://github.com/stefanberger/libtpms/releases/download/v0.10.2/libtpms-0.10.2.tar.gz
https://github.com/stefanberger/swtpm/releases/download/v0.10.1/swtpm-0.10.1.tar.gz
