# Append to the repo .gitignore — the official tree's derived state lives
# ONLY in these three (design doc §2): `git status` staying clean after a
# full ladder build is itself a hermeticity check.
/in/
/build/
/out/
